<div class="container">
<h1 class="title">Search Results</h1>
<p>Your search for "<%= @search_terms %>" return <%= pluralize(@cupcakes.size, 'result')%>.</p>
<br><br>
<ul>
  <% @cupcakes.each do |cupcake| %>
    <li><%= link_to cupcake.name, cupcake %></li>
  <% end %>
</ul>
</div>